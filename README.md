# Project-0-
If you are working on computers - do this first: 

'git pull'

or if it's the first time working on a new machine:

'git clone <paste link from github>'

after adding files:

'git add'

after important changes 

'git commit -am'useful commit mesage'

to upload to github:

'git push origin main'

or just:

'git push'

remember to keep your web browser console opne for errors.